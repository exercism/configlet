#!/usr/bin/env sh

nimble install -d -Y

case "$OS" in
  windows)
    nimble build -d:release --passC:'-flto' --passL='-s' --passL='-static'
    ;;
  linux)
    nimble build -d:release --passC:'-flto' --passL='-s' --passL='-static'
    ;;
  mac)
    nimble build -d:release
    ;;
esac
