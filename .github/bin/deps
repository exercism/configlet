#!/usr/bin/env sh

printenv

case "$(os)" in
  windows)
    echo "On windows"
    ;;
  linux)
    echo "On linux"
    sudo apt-get install musl-dev musl-tools
    ;;
  macos)
    echo "On macos"
    ;;
esac
