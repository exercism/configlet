#!/usr/bin/env sh

# Extract the version from the tooling_webserver.nimble file
VERSION="v$(nimble dump | grep "version:" | cut -d':' -f 2 | tr -d ' "')"
# echo "VERSION=${VERSION}" >> "${GITHUB_ENV}"

echo "version: $VERSION"

# gh release create "v"
